# 125 Valid palindrome
remove all the special characters from a sentence and check the string is palindrome or not.

## Approach
- split the string with proper regex.
- join the string by spaces and convert into lowercase.
- then reverse it and compare to check palindrome.