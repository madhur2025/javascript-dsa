# 169 Majority Element
finc the majority element from an array of integer and return which integer is in majority.

## Approach
- created and empty object
- use object to know the frequency of the elements
- loop the object to return which element in high marjority